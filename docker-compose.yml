services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: riviere
      POSTGRES_PASSWORD: R1vi3re2026
      POSTGRES_DB: riviere
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
